/// Barrel: central Firestore + follow access for the modular profile system.
///
/// Implementation lives in [ProfileRepository] (`lib/repositories/profile_repository.dart`)
/// so routing, repositories, and legacy pages share one source of truth.
library;

export 'package:halo/repositories/profile_repository.dart';
